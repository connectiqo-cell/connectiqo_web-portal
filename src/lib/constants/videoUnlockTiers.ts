/** Ported from connectfront/src/utils/playBilling.js — must stay in sync with the DB check constraint on mentor_profiles.unlock_price. */
export const VIDEO_UNLOCK_PRICE_TIERS = [199, 299, 499, 799, 999] as const;
